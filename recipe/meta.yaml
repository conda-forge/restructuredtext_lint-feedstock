{% set name = "restructuredtext_lint" %}
{% set version = "2.0.0" %}
{% set sha256 = "0d0d6a6766eb38e25abec7655b4a7806ce9441d5365a68e656cbb691527e6a45" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps -vv
  number: 0
  entry_points:
    - rst-lint = restructuredtext_lint.cli:main
    - restructuredtext-lint = restructuredtext_lint.cli:main

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools

  run:
    - python >={{ python_min }}
    - docutils >=0.11,<1.0

test:
  requires:
    - python {{ python_min }}
  imports:
    - restructuredtext_lint
  commands:
    - rst-lint -h
    - restructuredtext-lint -h

about:
  home: https://github.com/twolfson/restructuredtext-lint
  license: Unlicense
  license_file: UNLICENSE
  license_family: Public-Domain
  summary: Lint reStructuredText files
  dev_url: https://github.com/twolfson/restructuredtext-lint

extra:
  recipe-maintainers:
    - dopplershift
